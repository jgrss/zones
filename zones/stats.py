import numpy as np


STAT_DICT = dict(mean=np.mean,
                               nanmean=np.nanmean,
                               median=np.median,
                               nanmedian=np.nanmedian,
                               std=np.std,
                               nanstd=np.nanstd,
                               var=np.var,
                               nanvar=np.nanvar,
                               sum=np.sum,
                               nansum=np.nansum,
                               min=np.min,
                               nanmin=np.nanmin,
                               max=np.max,
                               nanmax=np.nanmax)
